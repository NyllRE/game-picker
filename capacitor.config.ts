/** @format */

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'nuxt.ionic.playground',
	appName: 'nuxt-ionic-playground',
	webDir: '.output/public',
	bundledWebRuntime: false,
};

export default config;
